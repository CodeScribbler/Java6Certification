package _05_24;


import _05_24.util.Utility;

/*
 * 1. 	javac (jdk) und java (jre)
 *
 *   Was sollen wir lernen?
 *   		-wie man diese zwei Befehle auf dem Terminal ausführt (Optionen  und Argumente)
 *
 *   javac: -d, -cp bzw -classpath
 *   java: -D, -cp bzw -classpath
 *
 *   2. Klassenpfad (classpath), JAR File
 *   			-CLASSPATH -Umgebungsvariable, die Verzeichnisse bzw JAR-dateien beinhaltet für Klassen (class files)
 *   						-Klassen, die für die Komplierung bzw Ausführung unserer Programme benötigt werden, werden in
 *   							dieser Verezeichnisse durchsucht.
 *   3. Package und imports
 *
 *   4. Argumente (args), Systemeigenschaften
 *
 *
 *
 *
 *
 */
public class Development {
    public static void main(String[] args) {
        System.err.println("Development...");
        Utility.ut();
    }
}
